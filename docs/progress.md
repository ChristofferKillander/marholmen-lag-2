# Progress Log

Running, dated log of decisions and steps taken on this project. Newest entries at the bottom.

---

## 2026-09-05

- Kicked off the repo, added initial README describing the team, tech stack (Claude Design, OpenAPI spec, PostgreSQL), and the convention for AI agents to commit design/API/code artifacts and log progress here as work happens.
- Added "Vibe Check hackathon board.zip" (Claude Design export) and extracted it into `design/vibe-check/` — contains `Vibe Check v2.dc.html` (the current design), an earlier `Vibe Check.dc.html`, the `modernist` design-system bundle/styles, `ios-frame.jsx`, and `support.js`.
- Wrote `api/vibe-check.openapi.yaml` (OpenAPI 3.1) covering the backend implied by `Vibe Check v2.dc.html`: `GET/POST /vibes` (the wall + posting a vibe), `GET /pulse` (room energy, mood, colour board, hype leaders), and `GET /options` (palette/emoji/energy choices for the compose sheet). Live "simulate live" behaviour in the design is modeled as polling (`since` param) rather than a socket endpoint — worth revisiting if the team wants real-time push.
- Filled in the README's project description from the design (Vibe Check: a live "room energy" board — post a vibe, see it aggregate into a team energy meter, loudest colour, hype leaders), and added "weekend throwaway project" ground rules: push straight to `main`, pull often, since all four of us commit directly to `main` in parallel.
- Added the workshop's 8-step workflow (idea → design in Claude Design → OpenAPI spec as contract → backend from spec → Prisma + database → SwiftUI app → deploy to Vercel → wrap-up) to the README as a flexible guideline, and filled in the tech stack with SwiftUI, Prisma, and Vercel now that they're confirmed.
- Saved the original idea pitch that led to "Vibe Check" to `docs/idea-pitch.md` for reference — it also proposes 4 workstreams (canvas/frontend, realtime backend, vibe-input UI, extras) and a couple of alternative concepts the team considered.
- Swapped the app framework in the README from SwiftUI to React Native (workflow step 6 and Tech Stack).
- Installed the Vercel CLI and logged in (`christofferkillander`). Linked the repo to a new Vercel project (`marholmen-lag-2/marholmen-lag-2` under the existing `marholmen-lag-2` team scope) and connected the GitHub repo, so every push to `main` now triggers an automatic deployment. No backend scaffold exists yet, so Postgres provisioning and env setup are deferred until someone starts step 4 (backend from spec).
- Implemented the backend from `api/vibe-check.openapi.yaml` as Vercel serverless functions: `api/vibes.ts` (GET list + POST create), `api/pulse.ts` (GET aggregates), `api/options.ts` (GET static compose choices). `prisma/schema.prisma` adds a single `Vibe` table (Postgres); `lib/pulse.ts` computes team energy/mood/colour board/hype leaders fresh on each `/pulse` read rather than storing a separate aggregate. `lib/constants.ts` mirrors the design's actual `PALETTE`/`EMOJI`/`MOODS` arrays (pulled from `Vibe Check v2.dc.html`) so `/options` matches the UI exactly, including the mood formula (`MOODS[round(avgEnergy) - 1]`, clamped). Per the spec, `colorBoard` omits zero-count colours; `loudestColor` falls back to the first palette entry (count 0) when no vibes exist yet, since the spec marks it required. "Live" vibes are currently just "all rows in the table" — no TTL/expiry — worth revisiting if the wall should only show recent posts. Not yet run/tested: this sandbox has no Node/npm installed, so `npm install` / `prisma generate` / a local request haven't been exercised — next person to touch this should do a real smoke test. Still using polling (`since` param) for live updates, per the spec's existing note. Not yet deployed to Vercel or wired to a real Postgres instance.
- Built the React Native app (step 6) under `app/` — Expo TypeScript, no native toolchain needed. It's a from-scratch scaffold implementing `design/vibe-check/Vibe Check v2.dc.html` and `api/vibe-check.openapi.yaml`: the wall grid, pulse tab (colour board + hype leaders), room-energy header, bottom ticker, and the "drop your vibe" compose sheet + confetti burst, all ported 1:1 (exact palette/fonts/behaviour, including a custom pixel-accurate marquee and falling-confetti animation via RN `Animated` since the design's CSS `%`-transform tricks don't translate directly). `src/api/` is pluggable: it defaults to an in-memory mock (seeded with the design's own demo data, simulating teammate posts every ~7s) and switches to the real backend the moment `EXPO_PUBLIC_API_BASE_URL` is set (see `app/.env.example`) — no code changes needed once step 4's backend is deployed; it isn't live yet, so mock stays the default for now. Verified end-to-end via `expo start --web` in a browser preview (typecheck clean, both tabs, posting a vibe, confetti, live stats updating) — added `.claude/launch.json` so that preview is repeatable. Known gap: the compose sheet's "your face" emoji picker doesn't feed the bottom CTA button's emoji (hardcoded 🔥) — the design ties them together via one shared component; not worth the extra state-lifting for a hackathon MVP.
- Smoke-tested the backend end-to-end: `npm install` + `prisma generate` clean, `npx tsc --noEmit` clean, spun up a throwaway local Postgres (Docker) and `prisma db push`'d the schema, then hit all three endpoints directly (not through `vercel dev`, which refuses to run here — see below) with real requests. Confirmed: `/api/options` returns the design's actual palette/emoji/mood arrays; `/api/vibes` GET/POST work including validation (missing status, bad hex color, out-of-range energy all correctly 400); a posted vibe shows up in the wall and correctly updates `/api/pulse` (teamEnergy, mood, colorBoard, hypeLeaders all recompute); `since`/`limit` query params behave per spec. Fixed a stale comment in `lib/http.ts` (said "SwiftUI app", should say "React Native app"). Note for whoever runs this next: `npm run dev` (`vercel dev`) errors with "must not recursively invoke itself" when run from an AI agent/CI-like environment — that's Vercel CLI's own agent-detection guard, not a bug in this repo; a human running it interactively in a real terminal should be fine. Local test DB and its data were discarded after testing — nothing persists.
- Deployed the backend to Vercel for real. First deploy failed (`No Output Directory named "public" found` — this repo has no static frontend, and zero-config deploy expects one); fixed by adding a small `public/index.html` status page describing the API. Preview deploy then succeeded; `/api/options` worked immediately but `/api/vibes`/`/api/pulse` 500'd (`FUNCTION_INVOCATION_FAILED`) since no `DATABASE_URL` was configured yet. Turned off Deployment Protection (`ssoProtection`, via `vercel api PATCH /v9/projects/...` — the CLI has no dedicated command for it) since previews were requiring a bypass token, which would've blocked the app and teammates from hitting the API directly; fine to leave open for a weekend project. Provisioned Neon Postgres via `vercel integration add neon` (needed a one-time browser step to accept Neon's marketplace terms — everything else was CLI-automatable) — this wrote `DATABASE_URL` and friends into Vercel's env vars and `.env.local`. Ran `prisma db push` against the real Neon database, redeployed, and re-verified all three endpoints against it (including a real POST + pulse recompute, then deleted that test row so the live data stays empty for the actual event). Promoted to **production**: `https://marholmen-lag-2.vercel.app` — all endpoints verified live there too. Committed the Neon integration's agent-skills cache (`.agents/`, `.claude/skills/`, `skills-lock.json` — pinned via hash, meant to be shared like a lockfile) so everyone's AI agent gets the same Neon guidance. Next step for the app: set `EXPO_PUBLIC_API_BASE_URL=https://marholmen-lag-2.vercel.app` (see `app/.env.example`) to point it at the real backend instead of the mock.
- Wired the app into the same Vercel deployment as a shared web app (per the original pitch: "a shared web app where everyone... can drop their vibe"), reusing the existing zero-config `public/`-as-static-output mechanism rather than adding a `vercel.json` (lower risk to the already-verified prod deploy). Root `package.json`'s `build` script now runs `prisma generate && npm --prefix app ci && npm --prefix app run build:web`; the app's new `build:web` script runs `expo export --platform web --output-dir ../public --clear` with `EXPO_PUBLIC_API_BASE_URL=/api` baked in via `cross-env` (relative path, so it works on preview and prod domains alike) — this replaces the old hand-written `public/index.html` status page with the real app on every deploy. `public/` is now **generated build output**, not source — untracked it from git and gitignored `/public/` accordingly (it's regenerated fresh on every Vercel build). Caught a real bug while verifying: the first export silently baked in the *mock* client because Metro's transform cache doesn't key on env vars, so a stale no-env-var cache entry got reused — fixed by adding `--clear` to the build. Verified functionally (not by grepping the minified bundle, which was misleading) by serving the built `public/` locally and confirming it fires real `fetch` calls to `/api/vibes`, `/api/pulse`, `/api/options` (they 404 locally since there's no `/api` on a plain static server — expected; they'll resolve once deployed alongside the real functions). Added a second `.claude/launch.json` entry (`vibe-check-static`) to preview the production build the same way. Not yet pushed/deployed — that's next.
- In parallel (before pulling the above), also deployed the app as a website — but the wrong way: a **separate** Vercel project (`vibe-check-web`, rooted at `app/` via `app/vercel.json`) with `EXPO_PUBLIC_API_BASE_URL` set to the full cross-origin backend URL (`https://marholmen-lag-2.vercel.app`, no `/api` suffix). Two bugs as a result: (1) `src/api/client.ts` appends paths like `/vibes`/`/pulse` directly onto that base, so requests hit `/vibes` instead of `/api/vibes` — a 404, which the browser reports as a CORS failure since Vercel's 404 page carries no CORS headers (confirmed live by the user hitting exactly this in the browser console); (2) even fixed, it'd still be a needless second, cross-origin deployment duplicating what the same-origin approach above already does better. **Superseded** by the same-origin `public/` approach above. Deployed that unified build to production (`vercel deploy --prod` at the repo root — Vercel's Git auto-deploy hadn't actually fired for either of the two most recent pushes, so this needed a manual trigger); verified live: `/` serves the real app (not the old status page), `/api/*` responds, and the bundle's baked-in base is the relative `"/api"` string, not an absolute cross-origin URL — no CORS is possible now. Decided to leave the separate `vibe-check-web` project in place rather than delete it (not worth the risk of removing a resource mid-event), but nothing should link to it — `https://marholmen-lag-2.vercel.app` is the one URL for the app going forward.
- Cleaned up the live wall: deleted 5 duplicate `"you"`/`"Hello"` test posts (from testing the app against the real backend) directly from the Neon database, leaving the genuine posts (Linda, Arisu, Mattias, Christoffer) intact.
- Team called it done. Added `docs/vad-vi-lart-oss.md` (learnings, in Swedish — collaboration + technical takeaways from the whole build) and `docs/demo-manus.md` (a timed 2-minute demo script: hook, show the wall, live-post from a phone, brief "how we built it," close, plus a backup plan if the network misbehaves).
- Added `PlasmaBackground` — 6 slowly-roaming, softly-glowing colour blobs (react-native-svg `RadialGradient` circles, using the app's own `PALETTE` hexes) behind the app content, for extra demo polish. Pure `Animated` transforms (translate/scale, `useNativeDriver`) with independently-timed X/Y loops per blob so paths curve around rather than sliding back and forth on a line — cheap, and identical on native and web. Caught and fixed a real clipping bug while verifying in the browser preview: the root view had no `overflow: hidden`, so blobs bled past the app's own bounds onto the surrounding page; fixed, and increased the roam radius (~85-120px) since the first pass was too subtle to read as "moving." Verified visually (two screenshots several seconds apart, confirmed blobs shifted) via `expo start --web`, then shipped through the same production pipeline (push → Vercel rebuild of `public/`).
- Gave the plasma background a more "iOS aurora" feel per feedback: bumped blobs to 7, much bigger (430-520px) and more overlapping, richer gradient stops (up to 85% opacity at the core), and added a single `expo-blur` `BlurView` pass (intensity 70, tint "dark") over the whole blob layer — melts the separate circles into one continuous, smoothly-blended glowing wash instead of distinct glows. Same cost model as before (blur is one native/CSS pass, blobs still just cheap transform loops). Verified visually on both tabs, confirmed still animating, then shipped.
- Animated the "loudest colour" badge's transition in `RoomEnergyHeader`: previously it just snapped to a new colour/emoji/name whenever the leaderboard flipped. Now a fade (380ms) + bouncy spring pop (`Animated.spring`, combined multiplicatively with the existing idle breathe via `Animated.multiply`) fires whenever `loudestColor.hex` changes, skipped on first mount so it doesn't animate in from nothing. Verified by posting a vibe with a different colour via the compose sheet and watching the badge flip (also caught it flipping a few times on its own from the mock's simulated bot posts during testing) — no console errors.
- Made the deployed web app a real installable PWA. Also fixed something overdue: `app/assets/icon.png`/`favicon.png`/`splash-icon.png` were still create-expo-app's default placeholder logo (blue "A" boomerang) — replaced with an actual Vibe Check mark (a bold flat-white lightning bolt over four blurred PALETTE-colour blobs on the dark bg, echoing `PlasmaBackground`), designed as one SVG and rasterized to all needed sizes with `sharp` in a scratch dir (not a project dependency). Added `app/public/manifest.webmanifest` + `icon-192.png`/`icon-512.png`/`apple-touch-icon.png` (Expo copies `app/public/*` verbatim into the web export's output root — confirmed by inspecting the build output). Since Expo's classic (non-router) web export doesn't link a manifest or add Apple's "add to home screen" meta tags itself, added `app/scripts/inject-pwa-head.mjs` (idempotent) to patch them into the exported `index.html`, wired into `build:web` right after `expo export`. Verified end-to-end: ran the full `npm run build` pipeline, confirmed `manifest.webmanifest`/all icons/`favicon.ico` serve 200 from the built `public/`, and the injected tags are present in `index.html`. No service worker added (not needed for either platform's manual "add to home screen" flow — out of scope for now).
