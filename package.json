{
  "name": "vibe-check-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for Vibe Check, built from api/vibe-check.openapi.yaml. Vercel serverless functions + Prisma/PostgreSQL.",
  "scripts": {
    "dev": "vercel dev",
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.24",
    "prisma": "^6.1.0",
    "typescript": "^5.6.3"
  }
}
