# Set this once the real Vercel backend (built from api/vibe-check.openapi.yaml)
# is deployed, e.g. https://marholmen-lag-2.vercel.app/v1. Leave unset (or this
# file uncopied) to run entirely against the built-in in-memory mock backend
# (src/api/mockClient.ts) — no setup needed, seeded with demo data.
# EXPO_PUBLIC_API_BASE_URL=https://your-backend.example.com/v1
